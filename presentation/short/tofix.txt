2 слайд: классификация текстов

основные задачи и их приложения
    задача1 (приложение1)
    задача2 (приложение2)

как решаются: 
    с помощью трансформеров (краткое определение)

Мы будем использовать конкретный трансформер:
    причина 1
    причина 2
    причина 3

3 слайд: 
    убрать штуку про токены и мировой рекорд
    сделать табличку

    1) как обучать
    2) проблемы

4 слайд:
    
    убрать "в течение T шагов"

    CL состоит из:

    1) сортировка данных по МЕТРИКЕ сложности, можем использовать разные метрики
    2) просто семплирование
        пример: 

    определение

    на практике делается так:
    сортирем по длине
    строим пакеты данных
    область растет
    описать динамический процесс

5 слайд:
    хочется понять, как разные метрики сложности влияют на скорость обучения, добавить!
    для этого нужно научиться сравнивать

6 слайд:

    В большинстве работ изучается в задачах таких-то, но не изучается в таких-то

    Обычно, в качестве метрик используется

    Обычно в центре внимания стоит не скорость, а точность. Скорость остается в стороне.

8 слайд
    расшифровать TF-IDF

    вынести EE, TSE в отдельный слайд

    ввести обозначения

    таблица

    название & условное обозначение & ссылка

    Обычно во всех работах используются три первых метрик
    я подумал и предложил следующие

    tf-idf на основе
    EE,TSE на основе 
    модельная на основе
    а также среднее число токенов в слове

    как правило в данных задачах используются три первые наиболее очевидные метрики:....
    я подумал и решил использовать еще несколько метрик
    одна метрика из информационного поиска и связана с подсчетом tf-idf
    следующие 2 метрики основаны на кодировании текстов некоторой случайной величиной

    убрать таблицу

    следующая метриа - это модельная
    и среднее число токенов в слове

    на слайде просто таблица

9 слайд
    
    нам нужно для вычисления метрики нам нужно сосчитать так называемые статистики
    для ускорения этого процесса я делю датасет на несколько частей и считаю их
    на каждой части независимо, а затем сливаю их в одно хранилище

    итого убрать

10 слайд

    В качестве датасета взят BooksCorpus, из таблицы убрать датасет, BooksCorpus

    max delta <= 3k убрать

    переделать заголовок таблицы, добавить слово семплеры

    мы брали разные метрики, пытались достигать разные пороги

    слайд со сравнением метрик перетащить перед этим

    стремимся минимизировать функцию потерь, выставляем порог и сравниваем
    метрики используя разные семплеры

11 слайд

    до 3% в сравнении с базой

    увеличиваем точность

    в общем случае обучение по плану ничего не дает

12 слайд
    
    однако, трогательная история
    про CV на чистых не сработало, а на шумных сработало

    появилась идея проверить здесь, мб сработает

    я ввожу метрику уровень шума

    добавляем шум

    оказалось, что 

    если порог невысокий, то есть выигрышь, иначе гг
